# ============================================================
# 前端环境变量示例
# ------------------------------------------------------------
# 复制为 .env.local 后按需修改（.env.local 不会被提交到 Git）。
#
# ⚠️ 严禁在此处放置 API Key。
#    用户自己的模型 API Key 只能由用户在网页「API 设置」中手动输入，
#    绝不允许通过 VITE_* 环境变量注入（会被打包进前端产物，公开泄露）。
# ============================================================

# 后端转发地址（Cloudflare Worker 的 /api/chat 端点）
# 开发环境：http://localhost:8787/api/chat
# 生产环境：https://<你的-worker-名称>.<你的子域>.workers.dev/api/chat
VITE_API_ENDPOINT=http://localhost:8787/api/chat
