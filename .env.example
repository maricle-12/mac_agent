# ============================================================
# 前端环境变量示例
# ------------------------------------------------------------
# 复制为 .env.local 后按需修改（.env.local 不会被提交到 Git）。
#
# ⚠️ 严禁在此处放置 API Key。
#    用户自己的模型 API Key 只能由用户在网页「API 设置」中手动输入，
#    绝不允许通过 VITE_* 环境变量注入（会被打包进前端产物，公开泄露）。
# ============================================================

# 模型转发地址（/api/chat 端点）
#
# 本地免安装版（默认交付方式）不需要配置：构建时用 .env.portable 里的同源相对路径 /api/chat，
# 由程序内置的本地服务处理。
#
# 只有以下两种可选场景才需要设置：
#   开发调试：http://localhost:8787/api/chat（本地 wrangler 转发）
#   云端方案：https://<你的转发服务地址>/api/chat（历史方案，见 docs/legacy-cloudflare-deployment.md）
#
# 留空时前端会自动回退到同源 /api/chat，本地版不受影响。
VITE_API_ENDPOINT=
