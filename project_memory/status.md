# 当前状态

## 总体进度

**阶段 11 / 14 —— 已完成并验证通过（等待用户确认后进入阶段 12）**

## 阶段 1 ~ 10 ✅

骨架 / 静态 UI / API 设置持久化 / Worker / 非流式转发与测试连接 / SSE 解析与流式验证 /
前端真实流式 / 教师学生 Prompt / IndexedDB 历史记录 / 会话管理完善。详见 `decision.md`。

## 阶段 11：错误处理完善 ✅

- **新增 `src/components/common/ErrorBoundary.tsx`**：
  - 整页级别包在 `main.tsx`，渲染期异常不再白屏，兜底页提供「重新加载页面」
    并明确告知聊天记录不会丢失；
  - 单条消息级别（inline 模式）包在 `MessageBubble` 的 Markdown 渲染外层，
    一条消息渲染失败只影响这一条；
  - 有意不写 `console`（日志可能含用户内容）。
- **流式中途断开友好化**（`src/hooks/useChat.ts`）：
  - `ApiRequestError` → 直接使用 Worker 的中文文案；
  - 其他异常且有已生成内容 → 「生成过程中连接中断，**已保留已生成的内容**。可点击「重新生成」重试。」；
  - 其他异常且没有内容 → 「网络连接中断，请检查网络后重试。」。
- 已有能力复核：401/402/403/404/422/429/5xx 均映射为中文文案（Worker `ERROR_TEXT`），
  网络层错误、端点占位符、空闲超时、主动停止、未配置 Key 均有专门提示；
  停止 / 出错后仍可「重新生成」（阶段 7 修过）。
- README 新增「错误处理」章节：13 种场景 → 用户看到什么 → 界面表现 的对照表。

## 验证结果（全部通过）

- `npm run build`：零 TypeScript 错误。
- `npm run check:ui`（**27 项**）：
  - dev：**27/27 通过**；
  - **生产构建**（`.env.production.local` 指向本地 Worker）：**27/27 通过**。
  - 新增 2 项：
    1. **流式中途断开**：桩 SSE 先发两个增量再 `controller.error()` →
       断言**已生成内容保留**、显示「连接中断」与「已保留已生成的内容」、
       停止按钮消失、仍可「重新生成」、错误样式生效、**页面仍可继续使用**（能新建对话）；
    2. **异常内容不崩页**：非法 LaTeX（`$$\frac{}{$$`）与未闭合代码块照常流式返回 →
       断言内容仍渲染、侧边栏与输入框仍在、未触发整页兜底。
- **错误边界已实测验证**：临时在 `ChatView` 注入一个渲染期 `throw`，用生产构建确认
  兜底页正确出现（#root 非空、显示「页面出现了意外错误」/「重新加载页面」/「查看技术详情」），
  验证后已还原并删除临时脚本，确认无残留。
- `npm run check:sse`：30/30；`cd worker && check`：38/38。
- 无 `console.error`、无未捕获异常。

## 过程中的问题

新增的两个用例起初都超时：因为前面的「清除 API 配置」用例把 API Key 清掉了，
`useChat` 在本地短路、根本不发请求，桩 SSE 自然收不到调用。
已新增 `ensureApiKey(ctx)` 辅助函数，让依赖发请求的用例先补回 Key。

## 当前状态

- 后台任务：`pwsh-30`（前端 dev 5173）、`pwsh-35`（wrangler dev 8787）。
- 错误处理已覆盖需求文档 §26 列举的全部场景，并额外覆盖端点占位符、空闲超时、渲染期异常。

## 下一步（阶段 12：移动端适配）

阶段 2 已实现移动端抽屉与响应式布局，并在 ui-check 中有基础断言（侧边栏折叠、
菜单按钮、抽屉可开、无横向溢出）。本阶段补齐：
1. 更多机型尺寸：360×640、390×844、414×896、768×1024（平板）；
2. 软键盘弹出时输入区不被遮挡（`dvh` 单位 / `visualViewport`）；
3. 触摸目标尺寸（≥44px）与 `hover` 依赖的交互在触屏上的可用性
   （会话的 ⋯ 菜单在触屏上是否可点）；
4. 安全区（`env(safe-area-inset-*)`）适配刘海屏；
5. 移动端消息气泡与代码块的横向滚动；
6. 把移动端检查从「基础断言」升级为「多尺寸 + 触摸可用性」断言集。

## 风险与限制

- 真实流式链路尚未由代理亲自跑通（无 Key）。
- `.env.production` 仍是占位符，阶段 14 必须替换。
- 移动端仅验证过 390×844 与抽屉开关；触屏交互与软键盘遮挡尚未验证。
