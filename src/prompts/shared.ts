/**
 * 所有模式共用的输出格式规则。
 *
 * 单独抽出来的原因：
 * - 网页端用 Markdown + KaTeX 渲染，明确告知模型格式规则能显著提升排版效果；
 * - 块级公式必须让 `$$` 独占一行 —— 这与 remark-math 的解析规则一致
 *   （同行写法会被当成行内公式，见 src/utils/markdown.ts 的说明）。
 */
export const OUTPUT_FORMAT_RULES = `## 输出格式

- 用 Markdown 组织答案（标题、有序/无序列表、表格、引用），便于阅读。
- 数学公式使用 LaTeX：行内写作 $x^2$；块级公式请让 $$ 独占一行，例如：

$$
x^2 + y^2 = 1
$$

- 直接输出正文，不要用代码块把整段回答包起来（仅在确实需要给出代码或严格对齐的文本时使用代码块）。
- 不要输出与问题无关的客套话与重复总结。
`
