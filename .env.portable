# 便携版（Windows 免安装 exe / macOS 应用包）构建模式。
#
# 便携版里前端由本地服务（127.0.0.1）同源提供，因此走相对路径 /api/chat，
# 不依赖任何已部署的云端 Worker。两个平台共用这一份配置。
#
# 构建命令：npm run build:portable（由 npm run build:win / build:mac 自动调用）
VITE_API_ENDPOINT=/api/chat
