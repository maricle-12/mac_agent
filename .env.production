# 生产/云端构建的接口地址（可选，本项目默认不需要）。
#
# 本项目的正式交付方式是本地免安装应用：前端由本机服务同源提供，
# 走 .env.portable 的 /api/chat，完全不依赖任何已部署服务。
#
# 只有当你额外想把前端部署成公网静态站点（历史方案，见
# docs/legacy-cloudflare-deployment.md）时，才需要在这里填你自己的转发服务地址。
# 保持空值时，前端会回退到同源 /api/chat，本地版完全不受影响。
#
# ⚠️ 这里只能填接口地址，绝不能放任何 API Key（会被打包进前端产物）。
VITE_API_ENDPOINT=
